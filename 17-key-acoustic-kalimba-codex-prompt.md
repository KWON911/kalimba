# 17-Key Acoustic Kalimba Web App — Codex 구현 지시서

## 1. 목표

React + Vite + TypeScript 기반으로 **17음계 어쿠스틱 칼림바 연주 웹앱**을 구현한다.

이 앱은 다음 두 환경을 모두 지원해야 한다.

- **모바일 / 태블릿**
  - 가로모드 중심
  - 화면의 칼림바 건반을 직접 터치하여 연주
  - 멀티터치 지원
  - 두 손 연주 가능
- **PC**
  - 마우스 클릭으로 연주
  - 물리 키보드로 연주 가능
  - 여러 키 동시 입력으로 화음 연주 가능

앱의 핵심은 “실제 칼림바를 연주하는 느낌”이다.
단순한 피아노형 버튼 UI가 아니라, 실제 17키 칼림바의 금속 건반 배열과 목재 바디를 시각적으로 표현한다.

---

# 2. 기술 스택

다음 기술을 사용한다.

- React
- Vite
- TypeScript
- CSS 또는 CSS Module
- Web Audio API

가능하면 외부 UI 라이브러리는 사용하지 않는다.

오디오 재생은 일반 `<audio>` 태그를 반복 호출하는 방식보다
**Web Audio API + AudioBuffer** 방식을 우선 사용한다.

---

# 3. 기본 음계

C조 17키 칼림바 기준으로 구현한다.

낮은 음부터 높은 음:

- C4
- D4
- E4
- F4
- G4
- A4
- B4
- C5
- D5
- E5
- F5
- G5
- A5
- B5
- C6
- D6
- E6

실제 칼림바 화면의 왼쪽 → 오른쪽 배치는 다음을 사용한다.

- D6
- B5
- G5
- E5
- C5
- A4
- F4
- D4
- C4
- E4
- G4
- B4
- D5
- F5
- A5
- C6
- E6

가운데 C4 건반이 가장 길고,
바깥쪽으로 갈수록 건반 길이가 점차 짧아지는 형태로 표현한다.

---

# 4. 숫자 표기

각 음에는 칼림바에서 사용하는 숫자 악보 표기를 함께 정의한다.

- C4 = 1
- D4 = 2
- E4 = 3
- F4 = 4
- G4 = 5
- A4 = 6
- B4 = 7
- C5 = 1°
- D5 = 2°
- E5 = 3°
- F5 = 4°
- G5 = 5°
- A5 = 6°
- B5 = 7°
- C6 = 1°°
- D6 = 2°°
- E6 = 3°°

표시 옵션을 통해 다음 중 하나를 선택할 수 있게 설계한다.

- 숫자
- 음이름
- 숫자 + 음이름
- 표시 안 함

1차 구현에서는 기본값을 **숫자 + 음이름**으로 한다.

예:

1
C4

---

# 5. 키보드 매핑

PC 키보드 연주를 지원한다.

화면의 물리적 위치와 최대한 자연스럽게 대응하도록
17개 건반을 다음 키와 연결한다.

왼쪽 → 오른쪽:

D6  = Q
B5  = W
G5  = E
E5  = R
C5  = T
A4  = Y
F4  = U
D4  = A
C4  = S
E4  = D
G4  = F
B4  = G
D5  = H
F5  = J
A5  = K
C6  = L
E6  = ;

각 건반 하단 또는 상단에 PC 키보드 문자를 작게 표시한다.

키보드 입력은 다음 조건을 만족해야 한다.

- `keydown` 시 재생
- `keyup` 시 눌림 상태 해제
- `event.repeat === true`인 자동 반복 입력은 무시
- 여러 키 동시 입력 허용
- 입력창이 활성화된 경우 키보드 연주가 방해되지 않도록 처리

---

# 6. 오디오 시스템

## 중요

이 앱의 가장 중요한 부분은 오디오 지연 최소화이다.

음원을 누를 때마다 파일을 새로 읽지 않는다.

앱 초기화 시 17개의 오디오 파일을 미리 로드하고,
`AudioBuffer`로 디코딩하여 메모리에 저장한다.

구조 예시:

public/
  sounds/
    C4.wav
    D4.wav
    E4.wav
    F4.wav
    G4.wav
    A4.wav
    B4.wav
    C5.wav
    D5.wav
    E5.wav
    F5.wav
    G5.wav
    A5.wav
    B5.wav
    C6.wav
    D6.wav
    E6.wav

음원이 아직 없는 경우에도 앱 전체가 깨지지 않도록 한다.

### 음원 fallback

실제 wav 파일을 찾지 못하면
Web Audio API의 OscillatorNode를 이용한 간단한 임시 음으로 재생한다.

단,
fallback은 개발용이며
실제 앱에서는 샘플 음원이 우선이다.

---

# 7. AudioContext 처리

모바일 브라우저 특히 iOS Safari 대응을 고려한다.

브라우저 정책 때문에 AudioContext가 suspended 상태일 수 있으므로

사용자의 첫 번째

- pointerdown
- touch
- click
- keydown

중 하나에서 AudioContext를 resume한다.

앱 시작부터 소리가 자동 재생되도록 시도하지 않는다.

---

# 8. 동시에 여러 음 재생

칼림바는 화음 연주가 가능하므로
동시에 여러 건반을 눌러도 각각 별도의 AudioBufferSourceNode가 생성되어야 한다.

예:

C4 + E4 + G4

동시 연주 가능.

한 음이 끝나기 전에 같은 음을 다시 눌러도
이전 소리를 강제로 끊지 말고 새로운 source를 만들어 겹쳐서 재생한다.

---

# 9. 모바일 터치

모바일에서는 `click` 중심으로 구현하지 않는다.

Pointer Events를 우선 사용한다.

주요 이벤트:

- pointerdown
- pointerup
- pointercancel
- pointerleave

건반 터치 즉시 소리가 나야 한다.

CSS:

- `touch-action: none`
- 연주 영역에서 텍스트 선택 방지
- 드래그로 페이지가 움직이지 않도록 처리

---

# 10. 멀티터치

반드시 멀티터치를 지원한다.

예:

왼손으로 C4
오른손으로 G4

동시 터치 시 두 음이 동시에 재생되어야 한다.

한 손가락의 pointerId가 다른 손가락 상태를 덮어쓰면 안 된다.

필요하면 각 건반별 active pointer set 또는
pointerId → note 매핑을 관리한다.

---

# 11. 슬라이드 연주

가능하면 다음 연주 방식도 지원한다.

손가락을 화면에 댄 상태에서
여러 건반 위를 좌우로 미끄러뜨리면
지나가는 건반의 음이 순차적으로 재생된다.

예:

C4 → D4 → E4 → F4 → G4

단,
같은 건반 영역 안에서 계속 움직일 때
음이 무한 반복되지 않도록 한다.

이 기능 때문에 `pointerenter` 또는
좌표 기반 hit test가 필요하다면 안정적인 방법으로 구현한다.

슬라이드 연주가 멀티터치 안정성을 해치면
1차에서는 제외해도 된다.

---

# 12. 화면 방향

앱은 모바일 가로모드 사용을 우선한다.

세로모드에서는 앱이 망가지지 않아야 한다.

세로모드일 경우:

“칼림바는 가로 화면에서 연주하기 편합니다.
기기를 가로로 돌려주세요.”

라는 안내 오버레이를 표시한다.

단,
강제로 화면 회전을 시도하지 않는다.

태블릿 또는 충분히 넓은 화면에서는
세로에서도 사용할 수 있다면 그대로 표시해도 된다.

---

# 13. 전체화면

전체화면 버튼을 제공한다.

가능한 브라우저에서는 Fullscreen API를 사용한다.

버튼 예:

⛶ 전체화면

전체화면을 지원하지 않는 브라우저에서는
앱이 오류 없이 정상 동작해야 한다.

---

# 14. 화면 디자인

## 전체 방향

실제 어쿠스틱 칼림바 느낌.

디자인 키워드:

- warm
- acoustic
- wood
- minimal
- realistic
- calm
- premium instrument

피아노 앱처럼 만들지 않는다.

---

# 15. 칼림바 바디

화면 중앙에 큰 목재 칼림바 본체를 배치한다.

특징:

- 둥근 모서리
- 자연스러운 목재 느낌
- 과도한 사실적 텍스처는 금지
- CSS gradient 정도로 표현
- 중앙 아래쪽에 sound hole 표현
- 얇은 그림자
- 화면을 최대한 넓게 활용

모바일 가로모드에서는
칼림바가 화면 높이 대부분을 사용하도록 한다.

---

# 16. 금속 건반

17개 금속 tine을 세로 방향으로 배치한다.

특징:

- silver / brushed metal 느낌
- 중앙 건반이 가장 길다
- 좌우로 갈수록 짧아진다
- 실제 칼림바처럼 중앙 C4 기준 대칭 느낌
- 건반 사이 간격은 손가락 터치가 가능한 수준
- 시각적 틈보다 실제 터치 영역은 조금 넓게 확보

터치 영역 최소 목표:

- 모바일 기준 약 40px 이상
- 가능하면 44px 이상

---

# 17. 건반 눌림 효과

건반을 누르면 즉시 시각 피드백이 있어야 한다.

효과:

- 아래로 2~4px 이동
- 밝기 또는 highlight 변화
- 아주 약한 scale 변화
- 약한 vibration 느낌의 짧은 animation

애니메이션은 짧고 빠르게 한다.

약 60~120ms.

과도한 bounce 효과는 사용하지 않는다.

---

# 18. 활성 건반 표시

현재 눌린 건반은 일반 건반과 명확하게 구분한다.

가능한 표현:

- 금속 highlight
- 은은한 glow
- 약간 밝아짐
- 음표 주변 강조

색상은 지나치게 화려하지 않게 한다.

---

# 19. 컨트롤 영역

화면 상단 또는 하단에 최소한의 컨트롤을 둔다.

필수:

- 앱 제목
- 음량 조절
- 음표 표시 방식
- 전체화면
- 설정

예:

17-Key Kalimba

[🔊 80%] [표시: 숫자+음이름] [⛶]

모바일에서는 공간을 많이 차지하지 않도록 한다.

---

# 20. 음량 조절

Master GainNode를 사용하여 전체 음량을 조절한다.

범위:

0 ~ 100%

기본:

80%

각 음원 source는 master gain으로 연결한다.

AudioBufferSourceNode
→ GainNode
→ destination

---

# 21. 햅틱

모바일에서 지원 가능한 경우
건반 터치 시 아주 짧은 진동을 선택적으로 사용할 수 있다.

예:

navigator.vibrate(8)

단:

- 기본 OFF 권장
- 미지원 브라우저에서 오류 발생 금지
- 설정에서 켜고 끌 수 있게 한다.

---

# 22. 상태 관리

1차 버전에서는 Redux 등 외부 상태 관리 라이브러리를 사용하지 않는다.

React state / hooks만 사용한다.

추천 hook:

- useKalimbaAudio
- useKeyboardControls
- useOrientation

---

# 23. 추천 컴포넌트 구조

src/

components/
  Kalimba.tsx
  KalimbaKey.tsx
  Controls.tsx
  PortraitNotice.tsx

hooks/
  useKalimbaAudio.ts
  useKeyboardControls.ts
  useOrientation.ts

data/
  notes.ts

types/
  kalimba.ts

App.tsx
main.tsx
styles.css

필요하면 구조는 개선해도 된다.

---

# 24. notes.ts 예시 데이터 구조

다음 정보를 포함한다.

```ts
export interface KalimbaNote {
  note: string;
  number: string;
  keyboard: string;
  frequency: number;
  file: string;
  order: number;
  lengthRatio: number;
}
```

예:

```ts
{
  note: "C4",
  number: "1",
  keyboard: "S",
  frequency: 261.63,
  file: "/sounds/C4.wav",
  order: 8,
  lengthRatio: 1
}
```

---

# 25. Frequency fallback

실제 음원이 없을 때 사용할 주파수:

C4 = 261.63
D4 = 293.66
E4 = 329.63
F4 = 349.23
G4 = 392.00
A4 = 440.00
B4 = 493.88

C5 = 523.25
D5 = 587.33
E5 = 659.25
F5 = 698.46
G5 = 783.99
A5 = 880.00
B5 = 987.77

C6 = 1046.50
D6 = 1174.66
E6 = 1318.51

---

# 26. 반응형

### PC

- 화면 중앙 정렬
- 최대 너비 설정 가능
- 키보드 매핑이 잘 보이도록 한다.

### 태블릿

- 화면 대부분을 칼림바가 차지
- 두 손 연주 가능

### 모바일 가로

- 최우선 대상
- 별도 스크롤 없이 한 화면에서 연주 가능
- 컨트롤은 작게
- 건반 터치 영역 최대화

---

# 27. 접근성

각 건반에는 다음 정보를 제공한다.

예:

```html
aria-label="C4, 숫자 1, 키보드 S"
```

가능하면 button 역할 또는 적절한 aria role을 사용한다.

키보드 포커스가 가능해야 한다.

단,
기본 focus outline을 완전히 제거하지 않는다.

---

# 28. 오류 대응

다음 상황에서도 앱이 깨지지 않아야 한다.

- 음원 파일 없음
- AudioContext 생성 실패
- Fullscreen API 미지원
- vibration API 미지원
- 모바일 Safari
- PC Chrome
- Edge
- 화면 크기가 매우 작음

사용자에게 치명적 오류 화면 대신
가능한 범위에서 fallback으로 계속 연주할 수 있게 한다.

---

# 29. 개발 중 테스트 음원

실제 칼림바 wav 파일을 프로젝트에 제공하지 않은 상태라면
앱 구현 자체를 멈추지 않는다.

우선 Oscillator fallback으로
모든 17음이 정확하게 동작하도록 한다.

향후 `/public/sounds/`에 wav 파일을 넣으면
자동으로 샘플 음원을 우선 사용하도록 만든다.

---

# 30. 샘플 음원 라이선스

프로젝트에 임의의 인터넷 음원을 다운로드하거나
저작권이 불명확한 파일을 포함하지 않는다.

음원은 사용자가 직접 제공하거나
명확한 라이선스가 있는 파일만 사용한다.

---

# 31. 1차 버전 범위

이번 작업에서는 다음 기능까지 완성한다.

필수:

1. 17개 칼림바 건반
2. 실제 칼림바 형태 UI
3. 모바일 터치 연주
4. 멀티터치
5. PC 마우스 연주
6. PC 키보드 연주
7. 여러 음 동시 재생
8. Web Audio API
9. sample preload
10. sample 없을 경우 oscillator fallback
11. 음량 조절
12. 음이름 / 숫자 표시
13. 전체화면
14. 가로모드 안내
15. 건반 눌림 animation
16. 반응형 디자인

---

# 32. 이번 버전에서 제외

다음 기능은 구조만 확장 가능하게 하고
지금은 구현하지 않는다.

- 노래 따라치기
- 숫자 악보 플레이어
- MIDI
- 녹음
- 연주 저장
- Supabase
- 로그인
- 사용자 계정
- 악보 업로드
- 자동 연주

불필요한 기능을 선제적으로 추가하지 않는다.

---

# 33. 향후 확장을 고려한 구조

향후 다음 기능을 붙이기 쉽게 만든다.

## 연습 모드

곡을 선택하면
다음에 눌러야 할 칼림바 건반이 빛나는 기능.

## 숫자 악보

예:

1 1 5 5 6 6 5
4 4 3 3 2 2 1

## 곡 데이터 구조

향후 다음과 같은 구조를 사용할 수 있게 한다.

```ts
interface SongNote {
  note: string;
  duration?: number;
}

interface Song {
  id: string;
  title: string;
  notes: SongNote[];
}
```

현재는 구현하지 않는다.

---

# 34. 성능

매 건반 입력마다 React 전체 컴포넌트가 불필요하게 재렌더링되지 않도록 한다.

오디오 객체는 React state보다 ref를 우선 고려한다.

예:

- AudioContext
- AudioBuffer map
- GainNode
- active sources

등은 useRef 사용을 검토한다.

---

# 35. 터치 지연 최소화

사용자가 건반을 누르는 순간
시각 효과보다 오디오 재생을 우선 처리한다.

입력 이벤트:

pointerdown

즉시:

playNote(note)

그 다음 active 상태 반영.

불필요한 debounce/throttle을 사용하지 않는다.

---

# 36. 모바일 브라우저 스크롤 방지

칼림바 연주 중 화면이 움직이면 안 된다.

연주 영역에서:

```css
touch-action: none;
user-select: none;
-webkit-user-select: none;
-webkit-tap-highlight-color: transparent;
```

등을 적절하게 적용한다.

단,
전체 페이지 접근성을 해치지 않도록
필요 영역에만 적용한다.

---

# 37. 키 반복 방지

PC에서 키를 길게 눌렀을 때
운영체제의 key repeat 때문에 음이 빠르게 반복되지 않도록 한다.

```ts
if (event.repeat) return;
```

을 적용한다.

같은 키는 keyup 전까지 active 상태를 유지한다.

---

# 38. 건반 길이

17개 건반은 배열 데이터에서
`lengthRatio`를 갖도록 한다.

C4를 가장 길게 하고
중앙에서 멀어질수록 단계적으로 짧게 한다.

정확한 물리 치수보다
시각적 자연스러움을 우선한다.

예시:

D6 0.60
B5 0.64
G5 0.68
E5 0.72
C5 0.76
A4 0.82
F4 0.88
D4 0.94
C4 1.00
E4 0.94
G4 0.88
B4 0.82
D5 0.76
F5 0.72
A5 0.68
C6 0.64
E6 0.60

필요하면 실제 칼림바처럼 더 자연스럽게 조정한다.

---

# 39. 디자인 세부

목재 바디:

- 따뜻한 갈색
- subtle gradient
- 과한 패턴 금지

금속 tine:

- 밝은 회색
- 위쪽에 약간 어두운 clamp/bridge
- highlight 표현

sound hole:

- 중앙 하단
- 원형
- 너무 크지 않게

로고:

KALIMBA

또는

17-KEY KALIMBA

정도로만 표현한다.

---

# 40. 첫 실행

앱 첫 실행 시 별도 시작 화면을 만들 필요는 없다.

바로 칼림바가 보이게 한다.

AudioContext 활성화를 위해
첫 터치가 필요한 경우에도

“Start”

같은 별도 화면으로 막지 않는다.

첫 건반 터치 자체로 활성화한 뒤
즉시 소리가 나도록 시도한다.

---

# 41. 로딩

17개 샘플을 로딩 중일 때
작은 상태 표시만 한다.

예:

Loading sounds…

샘플 일부가 실패해도
앱 전체 로딩을 실패 처리하지 않는다.

성공한 음은 sample,
실패한 음은 oscillator fallback을 사용한다.

---

# 42. 코드 품질

다음을 지킨다.

- TypeScript `any` 남용 금지
- 의미 있는 타입 정의
- 컴포넌트 책임 분리
- 거대한 App.tsx 하나에 전부 넣지 않기
- 불필요한 라이브러리 설치 금지
- 콘솔 오류 제거
- lint/build 오류 제거
- 사용하지 않는 코드 제거

---

# 43. 구현 순서

다음 순서로 작업한다.

### Step 1
현재 프로젝트 구조 분석

### Step 2
17음 note data 정의

### Step 3
Web Audio hook 구현

### Step 4
keyboard controls 구현

### Step 5
Kalimba / KalimbaKey UI 구현

### Step 6
pointer + multitouch 처리

### Step 7
volume / label controls

### Step 8
orientation / fullscreen

### Step 9
responsive 디자인

### Step 10
build 및 오류 수정

---

# 44. 기존 프로젝트가 있는 경우

현재 프로젝트 파일과 설정을 먼저 확인한다.

이미 존재하는:

- Vite
- React
- TypeScript
- ESLint
- CSS 구조

를 최대한 유지한다.

불필요하게 프로젝트를 새로 초기화하지 않는다.

기존 코드 중 이번 앱과 무관한 기능을 임의로 삭제하지 않는다.

---

# 45. 완료 후 반드시 확인할 테스트

## PC Chrome

- Q~; 건반 모두 동작
- 두 키 이상 동시에 눌러 화음 가능
- key repeat 방지
- 마우스 클릭 가능

## 모바일

- 가로 화면 정상
- 각 건반 터치 가능
- 두 손가락 동시 연주 가능
- 페이지가 연주 중 스크롤되지 않음
- 터치 시 지연 최소화

## 오디오

- 17음 모두 재생
- sample 존재하면 sample 사용
- sample 없으면 fallback
- 같은 음 연타 가능
- 이전 음이 자연스럽게 decay

## UI

- 17개 건반 모두 화면에 표시
- C4가 가장 길다
- 버튼이 화면 밖으로 나가지 않는다
- 작은 모바일에서도 레이아웃이 깨지지 않는다

---

# 46. Build 확인

작업 종료 전 반드시 실행한다.

```bash
npm run build
```

오류가 있으면 수정한다.

가능한 경우 lint도 실행한다.

```bash
npm run lint
```

기존 프로젝트에 lint script가 없다면
lint를 새로 추가하기 위해 불필요한 패키지를 설치하지 않는다.

---

# 47. 작업 결과 보고

완료 후 다음 형식으로 간단히 보고한다.

## 구현 완료

- 구현한 핵심 기능
- 추가/수정한 주요 파일
- 오디오 샘플을 넣을 위치
- PC 키보드 매핑
- 테스트 결과
- 남아 있는 제한 사항

긴 설명보다 실제 변경 내용을 중심으로 보고한다.

---

# 48. 가장 중요한 구현 원칙

이 프로젝트의 우선순위는 다음과 같다.

1. 연주 입력 반응 속도
2. 멀티터치 안정성
3. 자연스러운 칼림바 레이아웃
4. 실제 음원 사용 가능 구조
5. 모바일 가로 화면 사용성
6. PC 키보드 연주성
7. 시각적 완성도

화려한 애니메이션보다
**“누르는 순간 바로 소리가 나는 것”**을 우선한다.

---

# 최종 요청

위 요구사항을 기준으로 현재 프로젝트를 분석한 뒤
17-Key Acoustic Kalimba 웹앱을 실제로 구현하라.

작업 도중 사용자에게 코드를 붙여넣으라고 요구하지 말고
수정 가능한 프로젝트 파일은 직접 수정한다.

기존 구조를 최대한 유지하면서 필요한 파일을 추가/수정하고,
마지막에 `npm run build`까지 성공시키고 결과를 보고한다.
